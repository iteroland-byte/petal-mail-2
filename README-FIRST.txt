PETAL POST — CORRECTED GARDEN UPDATE

IMPORTANT:
Use the file named petal-post-app.js in this folder as your website's shared JavaScript file.

The code you pasted into ChatGPT had been converted into Markdown. That changed Firebase import URLs into Markdown links and escaped JavaScript characters, so the browser could not run the app. This copy is restored to real JavaScript.

Replace your existing:
    petal-post-app.js
with the one supplied here.

Then copy the entire assets/garden/ folder into your website's assets/ folder.

Do NOT rename the files in assets/garden/.

The update preserves the existing flower chooser/Firebase logic and includes the requested garden artwork and animation changes.
